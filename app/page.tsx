// Placeholder for T03 — T04 will replace with editor shell (chat + preview).
// Keeping a minimal page so `npm run build` passes for T03 verification.
export default function Page() {
  return (
    <main className="flex min-h-screen items-center justify-center p-8">
      <p className="text-slate-500 text-sm">
        Editor placeholder — run T04 for full chat + preview. Public pages live at /s/[slug].
      </p>
    </main>
  );
}
